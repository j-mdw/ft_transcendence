Docker Compose commands:

starting containers:
	normal start:
	- docker-compose up
	detach mode:
	- docker-compose up -d
	building all containers before starting them:
	- docker-compose up --build

stopping containers:
- docker-compose down
